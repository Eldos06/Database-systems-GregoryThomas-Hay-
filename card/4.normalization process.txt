

**a. Explain what is meant by “referential integrity.”** | Referential integrity means that relationships between tables remain consistent and valid. It ensures that every foreign key in one table correctly points to an existing primary key in another table. For example, if we have a “Trip” table and a “Driver” table, every Trip must reference a valid DriverID that exists in the Driver table. This rule prevents “orphan” records and guarantees logical consistency across the database. When referential integrity is enforced, deleting or changing a record automatically triggers rules (like `ON DELETE CASCADE`) to keep all related data synchronized. 	 **a. Объясните, что означает «ссылочная целостность».** | Ссылочная целостность означает, что связи между таблицами остаются согласованными и действительными. Она гарантирует, что каждый внешний ключ в одной таблице ссылается на существующий первичный ключ в другой таблице. Например, если есть таблица «Trip» и таблица «Driver», каждая поездка должна ссылаться на действительный DriverID из таблицы водителей. Это предотвращает появление «осиротевших» записей и обеспечивает логическую согласованность данных. При включённой ссылочной целостности удаление или изменение записи вызывает автоматические правила (`ON DELETE CASCADE`), которые поддерживают синхронность данных.
**b. Describe the difference between a natural key and a surrogate key. Why might one be preferred over the other?** | A natural key is a real-world attribute that uniquely identifies a record, such as a passport number or email address. A surrogate key, on the other hand, is an artificial identifier created by the database, like an auto-incrementing integer (e.g., `DriverID`). Surrogate keys are preferred in most modern systems because they are simple, never change, and make joins faster. Natural keys may seem intuitive but can change over time (a person might change their email or ID), causing cascading updates and inconsistency. Surrogate keys create stable relationships that simplify normalization and prevent duplicate data. 	 **b. Опишите разницу между естественным и суррогатным ключом. Почему один может быть предпочтительнее другого?** | Естественный ключ — это реальный атрибут, который уникально идентифицирует запись, например номер паспорта или адрес электронной почты. Суррогатный ключ — искусственный идентификатор, созданный базой данных, например автоинкрементное целое число (`DriverID`). В современных системах суррогатные ключи предпочтительнее, потому что они просты, неизменны и ускоряют соединения таблиц. Естественные ключи могут меняться со временем (например, пользователь сменит почту), что создаёт проблемы с обновлением. Суррогатные ключи обеспечивают стабильные связи и облегчают нормализацию.
**c. Explain what is meant by cardinality and provide an example.** | Cardinality describes the numerical relationship between two entities: how many instances of one can relate to another. The three common types are **one-to-one (1:1)**, **one-to-many (1:M)**, and **many-to-many (M:M)**. For example, one Driver can make many Trips (1:M), while many Passengers can take many Trips (M:M). Recognizing cardinality is crucial in normalization because it guides how we structure foreign keys and when to introduce associative entities. It ensures that relationships are neither duplicated nor missing, creating a logical, efficient schema. 	 **c. Объясните, что означает кардинальность, и приведите пример.** | Кардинальность описывает количественное отношение между двумя сущностями — сколько экземпляров одной может быть связано с другой. Существуют три типа: **один-к-одному (1:1)**, **один-ко-многим (1:M)** и **многие-ко-многим (M:M)**. Например, один водитель может совершать много поездок (1:M), а многие пассажиры могут участвовать во многих поездках (M:M). Понимание кардинальности важно для нормализации, потому что оно помогает правильно расставить внешние ключи и добавлять связывающие таблицы при необходимости. Это делает схему логичной и эффективной.
**d. Explain the purpose of associative entities and provide an example.** | Associative entities are bridge tables that resolve many-to-many (M:M) relationships into two one-to-many (1:M) relationships. They store the connection between two entities and often include their own attributes. For example, in the METRO_TRANSIT model, `TRIP_STOP` connects `TRIP` and `STOP`, recording when each vehicle stops at a location. Without associative entities, databases would contain duplicate or conflicting data. They are essential for full normalization (usually reaching 3NF or 4NF) because they eliminate redundancy and maintain referential integrity. 	 **d. Объясните назначение связывающих сущностей и приведите пример.** | Ассоциативные (связывающие) сущности — это промежуточные таблицы, которые преобразуют связь многие-ко-многим (M:M) в две связи один-ко-многим (1:M). Они хранят информацию о связи между двумя сущностями и часто содержат собственные атрибуты. Например, в модели METRO_TRANSIT таблица `TRIP_STOP` соединяет `TRIP` и `STOP`, фиксируя момент, когда транспорт останавливается в определённом месте. Без таких таблиц база данных содержала бы дублирующиеся или противоречивые данные. Ассоциативные сущности необходимы для полной нормализации (до 3NF или 4NF), так как устраняют избыточность и поддерживают ссылочную целостность.
**e. Show an example of an entity in UNF followed by the example in 3NF.** | In **Unnormalized Form (UNF)**, data might look like a spreadsheet: multiple values in one cell, inconsistent domains, or missing keys. For example, in METRO_TRANSIT, one record might include a column “DriverInfo” with both a name and phone number, and “Destination” with several stops in a single cell — a clear UNF design.
To achieve **Third Normal Form (3NF)**:
1️⃣ Separate repeating groups into individual entities (e.g., Driver, Destination).
2️⃣ Assign primary keys and remove multi-valued attributes.
3️⃣ Add associative tables for M:M relationships (e.g., `TRIP_STOP`).
4️⃣ Ensure that every non-key attribute depends only on the primary key.
Result: clear entities (`DRIVER`, `TRIP`, `STOP`), no duplicates, faster queries, and reliable updates — a model that is scalable and trustworthy. 	 **e. Покажите пример сущности в UNF и затем в 3NF.** | В **ненормализованной форме (UNF)** данные выглядят как таблица Excel: несколько значений в одной ячейке, разные типы данных и отсутствие ключей. Например, в METRO_TRANSIT запись может содержать столбец «DriverInfo» с именем и телефоном одновременно и «Destination» с несколькими пунктами — типичный UNF.
Чтобы достичь **третьей нормальной формы (3NF)**:
1️⃣ Разделить повторяющиеся группы в отдельные сущности (например, Driver, Destination).
2️⃣ Назначить первичные ключи и убрать многозначные атрибуты.
3️⃣ Добавить связывающие таблицы для M:M отношений (например, `TRIP_STOP`).
4️⃣ Убедиться, что каждый неключевой атрибут зависит только от первичного ключа.
Результат: чёткие сущности (`DRIVER`, `TRIP`, `STOP`), без дубликатов, быстрые запросы и надёжные обновления — масштабируемая и логичная модель.
**🌍 Summary** | The normalization process transforms chaotic, human-style data (like spreadsheets) into clean, machine-friendly structures. It prevents duplication, improves performance, and ensures data integrity through rules like referential integrity and normalization up to 3NF or 4NF. Following normalization is not just a rule — it is the embodiment of the “Organize or Die” principle within database design. 	 **🌍 Резюме** | Процесс нормализации преобразует хаотичные, «человеческие» данные (как в таблицах Excel) в чёткие структуры, понятные компьютеру. Он предотвращает дублирование, повышает производительность и обеспечивает целостность данных через правила ссылочной целостности и нормализации до 3NF или 4NF. Следование нормализации — это воплощение принципа «Организуйся или умри» в проектировании баз данных.


