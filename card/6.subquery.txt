✳️ 6. Explain the use of a subquery. | A subquery is a query written inside another SQL statement. It returns a temporary result set that the outer query uses to filter, compare, or calculate data. Subqueries make code more readable and reduce the need for complex joins. They are enclosed in parentheses and can appear in SELECT, FROM, or WHERE clauses. Example: SELECT Fname, Lname FROM tblDRIVER WHERE DriverID IN (SELECT DriverID FROM tblTRIP WHERE RouteID = 5); This command finds drivers who drove on route 5. Subqueries improve flexibility and allow dynamic filtering without rewriting the whole query. 	 ✳️ 6. Объясните использование подзапроса. | Подзапрос — это запрос, встроенный в другой SQL-оператор. Он возвращает временный набор данных, который внешний запрос использует для фильтрации, сравнения или вычислений. Подзапросы делают код более понятным и уменьшают количество сложных соединений. Они заключаются в круглые скобки и могут находиться в секциях SELECT, FROM или WHERE. Пример: SELECT Fname, Lname FROM tblDRIVER WHERE DriverID IN (SELECT DriverID FROM tblTRIP WHERE RouteID = 5); — выводит водителей, которые работали на маршруте 5. Подзапросы повышают гибкость и позволяют динамически фильтровать данные без переписывания всего запроса.

a. Explain how a subquery is often compared to a Venn Diagram. | A subquery behaves like a Venn Diagram because it represents the intersection or difference between two data sets. The inner query defines one circle (its own result set), and the outer query defines another. When combined with operators like IN, NOT IN, EXISTS, or NOT EXISTS, the database returns only rows that appear in both sets (intersection) or in one set but not the other (difference). This visual comparison helps learners understand how SQL filters overlapping or non-overlapping data between tables. 	 a. Объясните, почему подзапрос часто сравнивают с диаграммой Венна. | Подзапрос ведёт себя как диаграмма Венна, так как показывает пересечение или разницу между двумя множествами данных. Внутренний запрос — это один круг (его результат), а внешний — другой. При использовании операторов IN, NOT IN, EXISTS, NOT EXISTS СУБД возвращает строки, присутствующие в обоих множествах (пересечение) или только в одном (разность). Такое визуальное сравнение помогает понять, как SQL фильтрует пересекающиеся и непересекающиеся данные между таблицами.

b. Identify the different methods presented in class where a subquery can be used. | Subqueries can appear in several forms: 1. Scalar subquery — returns a single value, used in expressions or comparisons (SELECT AVG(Salary) ...). 2. Column or Row subquery — returns one column or row to compare sets of values. 3. Table subquery (derived table) — placed in the FROM clause; acts like a temporary table to simplify complex joins. 4. Correlated subquery — references columns from the outer query and executes once per row; often used with EXISTS. Each method provides different control over scope and performance, depending on the need for one value, a list, or a temporary table. 	 b. Назовите различные способы использования подзапросов. | Подзапросы могут применяться в нескольких формах: 1. Скалярный подзапрос — возвращает одно значение, используется в выражениях или сравнениях (SELECT AVG(Salary) ...). 2. Столбцовый или строковый подзапрос — возвращает один столбец или одну строку для сравнения множеств значений. 3. Табличный подзапрос (производная таблица) — помещается в секцию FROM и действует как временная таблица для упрощения соединений. 4. Коррелированный подзапрос — ссылается на столбцы внешнего запроса и выполняется для каждой строки; часто используется с EXISTS. Каждый тип даёт разный уровень контроля и производительности в зависимости от того, нужен ли один результат, список или временная таблица.

c. When might each be preferred? | 1. Scalar — when the outer query needs a single computed value (like average salary or max date). 2. Column/Row — when comparing a set of specific values (WHERE (column1, column2) IN (...)). 3. Table — when combining or aggregating data before joining with others. 4. Correlated — when filtering based on related data, such as “employees who earn more than the average salary in their department.” In practice, non-correlated subqueries are faster because they execute once, while correlated ones run per row but give context-sensitive filtering. 	 c. Когда предпочтительно использовать каждый тип? | 1. Скалярный — когда внешнему запросу нужно одно вычисленное значение (например, средняя зарплата или максимальная дата). 2. Столбцовый/строковый — при сравнении набора конкретных значений (WHERE (column1, column2) IN (...)). 3. Табличный — при необходимости агрегировать или объединить данные перед соединением с другими таблицами. 4. Коррелированный — при фильтрации по связанным данным, например: «сотрудники, зарабатывающие больше среднего по отделу». На практике некоррелированные подзапросы быстрее, так как выполняются один раз, тогда как коррелированные запускаются для каждой строки, но обеспечивают контекстную фильтрацию.

🌍 Summary | Subqueries add logic, hierarchy, and flexibility to SQL design. They replace multiple joins, isolate logic, and support dynamic comparisons. In relational databases, they reflect the “Organize or Die” principle by structuring queries into reusable layers — organizing complexity into smaller, purposeful blocks. 	 🌍 Резюме | Подзапросы добавляют логику, иерархию и гибкость в SQL-разработку. Они заменяют множественные соединения, изолируют логику и поддерживают динамические сравнения. В реляционных базах данных подзапросы воплощают принцип «Организуйся или умри», структурируя сложные запросы в небольшие функциональные блоки.